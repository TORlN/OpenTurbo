[metadata]
# Legacy setuptools compatibility stub. Canonical package metadata lives in pyproject.toml.
name = openturbo
license = MIT

[options]
package_dir =
    = src
packages = find:

[options.packages.find]
where = src
